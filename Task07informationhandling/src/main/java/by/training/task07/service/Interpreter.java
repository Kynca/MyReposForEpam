package by.training.task07.service;

import java.util.Stack;

/**
 * realise pattern interpreter
 * used for counting expressions
 */
public interface Interpreter {
   void interpret(Stack<Integer> stack);
}
